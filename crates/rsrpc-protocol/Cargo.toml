[package]
name = "rsrpc-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
authors = ["spikehd"]
description = "rsRPC wire protocol: errors, bridge payloads, reply builders, flood guard"
license = "MIT"
repository = "https://github.com/yiesko/rsRPC"
keywords = ["discord", "rich-presence", "rpc"]
categories = ["network-programming"]

[lints]
workspace = true

[dependencies]
rsrpc-types = { path = "../rsrpc-types" }
bytes = "1.12"
tungstenite = "0.30"
serde = { version = "1.0", features = ["derive", "rc"] }
serde_json = "1.0"
serde_with = "3.21"
rmp-serde = "1.3"
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"
rsrpc-types = { path = "../rsrpc-types" }
criterion = "0.8.2"

[[bench]]
name = "comparison"
harness = false
